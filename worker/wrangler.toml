name = "banking-sync"
main = "worker.js"
compatibility_date = "2024-09-01"

# KV binding, APP_ID, PWA_URL and the 2 secrets (APP_SECRET,
# APP_PRIVATE_KEY) are configured via the Cloudflare dashboard,
# not here, so this file stays portable.
